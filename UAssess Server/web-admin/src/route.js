import React from 'react';
import { Route} from 'react-router';
import App from '/';
//import Authentication from './pages/Authentication/Authentication';

export default (
 // <Route path="./pages/Authentication/Authentication" component={Authentication}>
 <Route path="./App/" component={App}>
 
  </Route>
  //<Route path='/users/:id' exact component={MyComponent} ></Route>

);